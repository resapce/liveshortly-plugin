{
  "name": "system-message",
  "version": "1.8.0",
  "description": "Injects a custom system message at session start, exposes tools to read/update it, and runs a two-way webhook channel",
  "author": {
    "name": "Mukul"
  },
  "mcpServers": {
    "system-message": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"]
    },
    "system-message-channel-npx": {
      "//": "Uses npx to run tsx — requires Node.js; tsx is fetched on demand if not installed globally",
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/channel/channel.ts"]
    },
    "system-message-channel": {
      "//": "Alternative: uses bun runtime directly — faster cold start, no tsx needed",
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/channel/channel.ts"]
    }
  }
}
