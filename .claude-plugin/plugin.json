{
  "name": "liveshortly",
  "version": "3.0.0",
  "description": "Live-stream your Claude Code session to LiveShortly: capture prompts/tools/edits, share sessions Drive-style, and let viewers message back \u2014 with browser-based sign-in.",
  "author": {
    "name": "Mukul"
  },
  "skills": "./skills/",
  "mcpServers": {
    "liveshortly-server": {
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"
      ]
    },
    "liveshortly-channel": {
      "//": "Alternative: uses bun runtime directly \u2014 faster cold start, no tsx needed",
      "command": "bun",
      "args": [
        "run",
        "${CLAUDE_PLUGIN_ROOT}/channel/channel.ts"
      ]
    }
  }
}
